package com.test.java;

public class Re_String01 {

	public static void main(String[] args) {
		
		
		
		//[SUMMARY] String 문자열 만들기
		
		
		//String XXX
		//1. length() > 문자열 길이
		
		//2. charAt(index) > 문자 추출
		
		//3. indexOf(search, index), lastIndexOf(search, index) > 위치 검색 (index 부분은 몇번째 위치부터 서치할건지 선택)
		
		//4. trim() > 공백 제거
		
		//5. toUpperCase(), toLowerCase() > 대소문자 변환
		
		//6. startsWith(String word), endsWith(String word) > 패턴 검색 > ~로 시작? 끝? > true, false
		
		//7. substring(being, end) > 문자열 추출
		
		//8. contains(word) > 문자열 검색 > true, false
		
		//9. replace(old, new) > 문자열 치환
		
		//10. split(String delimiter) > 구분자를 기준으로 나눈다.
	}

}
